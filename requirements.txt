# FastAPI backend
fastapi
uvicorn[standard]
pydantic

# ML + NLP

scikit-learn
joblib

# Environment handling
python-dotenv

# Django UI (optional)
Django>=5.0
## Render build step expects requirements.txt at the service root.
## This repo is a Django project under ./email_spam_project.
-r email_spam_project/requirements.txt